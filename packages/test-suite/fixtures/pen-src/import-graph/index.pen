(f as foo, b as bar, baz) = import './a'
() = import './b.pen'
(ascii) = import 'std'

start = result

digit = ascii(min="0" max="9")
alpha = ascii(min="a" max="z") | ascii(min="A" max="Z")

result = foo (bar baz)

myList = [digit, digit digit, digit digit digit]

rec = (
    b = 'b thing'
    d = 'd thing'
)

r2 = rec
r2d = rec.d
