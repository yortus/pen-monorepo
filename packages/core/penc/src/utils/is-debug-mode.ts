// TODO: temp testing... work out how best to determine debug mode properly. eg env var?
export function isDebugMode() {
    // TODO: fix this. Always return true for now, while compiler is under development
    return true;
}
