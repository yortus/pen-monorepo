{
    "private": true,
    "name": "pen",
    "version": "0.0.1",
    "description": "",
    "main": "dist/index.js",
    "typings": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p src",
        "postbuild": "pegjs -o dist/grammar.js src/grammar.pegjs",
        "clean": "rimraf dist/"
    },
    "dependencies": {
        "typescript": "next"
    },
    "devDependencies": {
        "pegjs": "^0.10.0"
    }
}
