{f as foo, b as bar, baz} = import './a'
{} = import './b.pen'

digit = "0-9"
alpha = "a-z" | "A-Z"

result = foo(bar baz)

myList = [digit, digit digit, digit digit digit]

rec = {
    b = 'b thing'
    d = 'd thing'
}

r2 = rec
r2d = rec.d
